# This script is run inside the jail after it is created and any packages installed. 
# Enable services in /etc/rc.conf that need to start with the jail and apply any configuration customizations with 
# this this script.
